ServerState - Singleton Class
- maintains the current state of the server (role, log, variables - i.e term... - etc.)
    - also handles incoming packets forwarded from the UDPServer.

UDPServer - Singleton Class
- handles incoming and outgoing packets
    - returns a buffer/message object holding corresponding message

Log - Singleton Class
- maintains the replicated log

RASPFS - Singleton Class, uses the SPIFF FS
    Files for ServerState:
        - `SS/currentTerm`
        - `SS/votedFor`
        - `SS/Log`
